# SnapPurge Android 開発計画

## 1. プロダクト概要

SnapPurge は、Android端末内のスクリーンショットを完全ローカルで解析・整理するアプリです。画像、OCR結果、分類情報、要約、検索語、リマインダー情報を外部サーバーへ送信せず、端末内だけで処理します。

- 対象: Android
- 技術: Kotlin + Jetpack Compose
- 作業ディレクトリ: `C:\Users\meita\project\snapshelf`
- 仮パッケージ名: `com.meita.snapshelf`
- 通信方針: 解析データを外部へ送信しない。原則として `INTERNET` 権限は付与しない。
- 初期リリース範囲: 本格版

## 2. 必須機能

### 整理・検索

- MediaStore からスクリーンショットを検出・取り込み
- ML Kit のオンデバイスOCRによる文字抽出
- OCR文字、撮影日時、アプリ由来情報、ユーザータグを対象にした全文検索
- 端末内ルールによるカテゴリ自動分類
- お気に入り、アーカイブ、複数選択、一括タグ付け
- 元画像の表示、共有、削除導線

### 高度なローカル機能

- OCR結果からのローカル要約
- 日時・期限候補の抽出とリマインダー作成
- perceptual hash を使った同一・類似画像の重複検出
- Android Sharesheet を使った共有
- WorkManager によるバックグラウンド索引更新
- Room + SQLite FTS によるローカル全文検索

### プライバシー

- 画像、OCRテキスト、検索語、分類情報を外部送信しない
- `AndroidManifest.xml` に `INTERNET` 権限を入れない
- 権限要求前に、処理範囲と理由をUIで明示
- Android 13以降は `READ_MEDIA_IMAGES`、Android 12以前は `READ_EXTERNAL_STORAGE` を使用
- 可能な場面では Photo Picker による限定選択も併用
- ローカルDB削除、索引再構築、キャッシュ削除を設定画面から実行可能にする

## 3. UI・デザイン方針

Material 3 を基盤に、写真一覧を主役にした静かでモダンなUIにします。

- テーマ: システム設定に追従 / ライト / ダーク の3択
- Dynamic Color: Android 12以降で任意に使用可能
- 一覧: 可変グリッド、角丸カード、控えめな階層表現、余白を活かしたレイアウト
- ホーム: 最近のスクショ、スマートカテゴリ、期限候補、重複候補
- 検索: 大きな検索欄、フィルタチップ、検索結果の即時更新
- 詳細: 画像、OCRテキスト、要約、タグ、リマインダー、共有操作
- 状態表現: スケルトン、空状態、エラー、権限拒否状態をすべて設計
- アクセシビリティ: TalkBack、48dp以上のタップ領域、文字サイズ拡大、十分なコントラスト
- アニメーション: 画面遷移と選択フィードバックに限定し、過剰にしない

## 4. 主要画面

1. オンボーディングと権限説明
2. ホーム / スマートコレクション
3. 全スクリーンショットのグリッド
4. 検索と絞り込み
5. スクリーンショット詳細
6. 重複候補の比較・整理
7. リマインダー一覧と編集
8. 設定、テーマ、プライバシー、索引管理

## 5. アーキテクチャ

- UI: Jetpack Compose + Material 3
- 状態管理: ViewModel + StateFlow
- 構成: UI / Domain / Data を分離した単一アプリモジュール
- DB: Room、FTSテーブル、Migrationテスト
- メディア取得: MediaStore / Photo Picker
- OCR: ML Kit Text Recognition のオンデバイスモデル
- バックグラウンド処理: WorkManager
- 設定: DataStore
- ナビゲーション: Navigation Compose
- 画像表示: Coil（ローカルURIのみ）
- テスト: JUnit、Room、Compose UI、必要範囲のインストルメンテーションテスト

初期段階では、OCR文字正規化、期限候補抽出、キーワード順位付けによる決定的なローカル処理を使います。端末内LLMはAPKサイズ、端末性能、ライセンスへの影響を計測したうえで後続ループの選択肢とし、必須要件にはしません。

## 6. ループエンジニアリングとサブエージェント活用

本開発では、役割ごとに異なるモデルを割り当てたサブエージェントを活用します。各ループは「調査・設計 → 実装 → 自動検証 → 独立レビュー → 修正 → 再検証」の順で進め、レビュー指摘が解消するまで反復します。

| 役割 | 使用モデル | 担当 |
|---|---|---|
| オーケストレーター / 統合実装 | 親エージェントの現行モデル | 要件管理、統合、競合回避、最終判断 |
| Androidアーキテクト | `gpt-5.6-sol` | Compose、Room、WorkManager、権限、設計レビュー |
| UI/UXデザイナー | `gpt-5.6-terra` | デザインシステム、画面状態、操作性、アクセシビリティ |
| QA・プライバシーレビュアー | `gpt-5.5` | テスト観点、権限、外部通信、有害系、回帰レビュー |

モデルが実行環境で利用できない場合は、利用可能な最も近いモデルへ切り替え、その変更理由と結果を本ファイルまたは開発ログに記録します。サブエージェントには重複するファイル編集を同時に行わせず、調査・レビューと明確に分離できる実装だけを並列化します。

## 7. 実装ループ

### Loop 0: 基盤とデザインシステム

- Gradleプロジェクト、Compose、Room、WorkManager、テスト基盤
- 色、タイポグラフィ、形状、間隔、テーマ選択、Dynamic Color
- ナビゲーションと主要画面の空状態
- 完了条件: Debugビルド、単体テスト、ライト/ダークのCompose表示確認が成功

### Loop 1: 取り込み、OCR、索引

- 権限、Photo Picker、MediaStore
- スクリーンショット検出と差分同期
- OCRパイプライン、Room保存、FTS検索
- 完了条件: サンプル画像の取り込みから検索まで端末内で動作

### Loop 2: 分類と日常整理

- スマートカテゴリ、タグ、お気に入り、アーカイブ、一括操作
- ホーム、グリッド、詳細、検索フィルタ
- 完了条件: 主要ユーザーフローのCompose UIテストが成功

### Loop 3: 本格機能

- ローカル要約、日時・期限抽出、リマインダー
- 重複・類似画像検出と比較UI
- 共有、削除、索引再構築
- 完了条件: 各機能の単体・統合テスト、および大量画像時の性能基準を満たす

### Loop 4: 品質仕上げ

- アクセシビリティ、回転、プロセス再生成、大きな文字、権限拒否
- 性能、メモリ、バッテリー、DB migration、プライバシー監査
- UIのスクリーンショット比較と最終調整
- 完了条件: 全テスト成功、重大レビュー指摘なし、外部通信なしを確認

## 8. 各ループの検証項目

- `assembleDebug` と単体テスト
- Android Lint とKotlinコンパイル
- Room DAO / FTS / migration テスト
- Compose UI テストと主要画面のスクリーンショット確認
- 権限許可、拒否、一部選択の各経路
- OCR失敗、破損URI、画像削除、低メモリ、再起動後の復元
- 大量画像での索引時間、検索応答、メモリ使用量
- Manifest と依存関係を確認し、意図しないネットワーク利用がないことを検証

## 9. 完了条件

- 要件に記載した主要機能が実機またはエミュレーターで一連動作する
- システム追従、ライト、ダークの全テーマで主要画面が読みやすい
- 主要ロジックと重要ユーザーフローに自動テストがある
- ビルド、テスト、Lint が成功する
- 画像および解析結果の外部送信がなく、プライバシー説明と実装が一致する
- 既知の制約と今後の改善候補が README に記録されている
